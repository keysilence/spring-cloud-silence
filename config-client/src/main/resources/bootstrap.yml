spring:
  application:
    name: config-client
  cloud:
    config:
      label: master
      profile: test
      uri: http://localhost:7788/
server:
  port: 7799
management:
  endpoints:
    web:
      exposure:
        include: refresh